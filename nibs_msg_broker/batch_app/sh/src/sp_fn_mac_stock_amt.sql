DECLARE
	PDATE VARCHAR2(200);
	P_STATUS VARCHAR2(200);

BEGIN
	SELECT TO_CHAR(SYSDATE - 1,'YYYYMMDD')
	INTO PDATE
	FROM DUAL;

	P_STATUS := NULL;

	NIBS.SP_FN_MAC_STOCK_AMT ( PDATE, P_STATUS );
	COMMIT;
END;
/
EXIT;
