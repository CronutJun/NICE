-- 나이스 과여금 집계

DECLARE 
  PDATE VARCHAR2(200);

BEGIN 
  SELECT TO_CHAR(SYSDATE- 1,'YYYYMMDD')
  INTO PDATE
  FROM DUAL;

  NIBS.SP_FN_NOTEEND_NICE ( PDATE );
  COMMIT; 
END; 

/
EXIT;
