-- 나이스 일일 말잔 집계

DECLARE 
  PDATE VARCHAR2(200);

BEGIN 
  SELECT TO_CHAR(SYSDATE- 1,'YYYYMMDD')
  INTO PDATE
  FROM DUAL;

  NIBS.SP_FN_NICE_ENDAMT ( PDATE );
  COMMIT; 
END; 

/
EXIT;
